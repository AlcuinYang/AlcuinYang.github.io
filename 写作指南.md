# 博客写作指南

本文档记录完整的博客写作流程，方便快速查阅。

---

## 一、新建文章

### 方法 1：使用命令（推荐）

```bash
hugo new content posts/文章标题.md
```

这会基于模板自动创建带 Front Matter 的文件。

### 方法 2：手动创建

在 `content/posts/` 目录下新建 `.md` 文件。

---

## 二、文章格式（Front Matter）

每篇文章开头必须包含：

```markdown
---
title: "文章标题"
date: 2024-03-24T10:00:00+08:00
draft: false
tags: ["标签1", "标签2"]
categories: ["分类"]
---
```

### 字段说明

| 字段 | 说明 | 示例 |
|------|------|------|
| `title` | 文章标题 | `"我的思考"` |
| `date` | 发布时间 | `2024-03-24T10:00:00+08:00` |
| `draft` | 草稿状态 | `false` 发布，`true` 草稿 |
| `tags` | 标签（可选） | `["随笔", "生活"]` |
| `categories` | 分类（可选） | `["思考"]` |

---

## 三、正文写作

使用标准 Markdown 语法：

```markdown
## 二级标题

正文段落。

**粗体**，*斜体*，`代码`。

### 列表

- 无序列表项
- 另一项

1. 有序列表
2. 第二项

### 引用

> 这是一段引用文字。

### 代码块

```python
print("Hello World")
```

### 链接

[链接文字](https://example.com)

### 图片

![图片描述](/images/photo.jpg)
```

---

## 四、本地预览

```bash
# 进入博客目录
cd hugo-new-blog

# 启动开发服务器
hugo server -D

# 打开浏览器访问
# http://localhost:1313
```

> **提示**：`-D` 参数包含草稿文章，正式发布前记得把 `draft` 改为 `false`。

---

## 五、发布流程

### 步骤 1：确认文章状态

检查文章 `draft: false`，且内容已完成。

### 步骤 2：停止本地服务器

按 `Ctrl + C` 停止 `hugo server`。

### 步骤 3：提交到 GitHub

```bash
# 添加所有更改
git add .

# 提交（写上描述性的提交信息）
git commit -m "Add new post: 文章标题"

# 推送到 GitHub
git push
```

### 步骤 4：等待自动部署

1. 打开 GitHub 仓库页面
2. 点击 Actions 标签
3. 看到绿色的 ✓ 即表示部署成功
4. 访问 https://alcuinyang.github.io 查看

---

## 六、完整示例

假设要写一篇名为「周末随想」的文章：

### 1. 创建文章

```bash
hugo new content posts/周末随想.md
```

### 2. 编辑内容

```markdown
---
title: "周末随想"
date: 2024-03-24T14:30:00+08:00
draft: false
tags: ["随笔", "生活"]
categories: ["日常"]
---

这个周末过得格外平静。

## 早上的咖啡

泡了一杯手冲，坐在窗边看了会儿书。

> "简单的生活，就是最大的幸福。"

## 下午的散步

沿着河边走了很久，春天的风很温柔。

![河边的柳树](/images/willow.jpg)

---

下周要开始新的项目了，期待又有些忐忑。
```

### 3. 本地预览

```bash
hugo server -D
# 浏览器打开 http://localhost:1313/posts/周末随想/
```

### 4. 发布

```bash
git add .
git commit -m "Add new post: 周末随想"
git push
```

---

## 七、常见问题

### Q1：文章不显示？

- 检查 `draft` 是否为 `false`
- 检查 `date` 是否为过去时间（未来时间不会显示）
- 确认文件保存在 `content/posts/` 目录下

### Q2：本地预览正常，但网站没更新？

- 检查 GitHub Actions 是否成功（绿色 ✓）
- 清除浏览器缓存，或按 `Ctrl + Shift + R` 强制刷新
- 等待 1-2 分钟（CDN 刷新需要时间）

### Q3：如何修改文章？

直接编辑 `.md` 文件，然后重新 `git add` → `git commit` → `git push`。

### Q4：如何删除文章？

```bash
# 删除文件
rm content/posts/文章标题.md

# 提交更改
git add .
git commit -m "Remove post: 文章标题"
git push
```

---

## 八、快捷命令备忘

```bash
# 新建文章
hugo new content posts/标题.md

# 本地预览
hugo server -D

# 提交并发布
git add . && git commit -m "xxx" && git push

# 仅构建（本地查看生成文件）
hugo
```

---

## 九、资源存放

- **图片**：放在 `static/images/` 目录
- **自定义 CSS**：放在 `assets/css/` 目录
- **自定义 JS**：放在 `assets/js/` 目录

---

**祝你写作愉快！** 📝
